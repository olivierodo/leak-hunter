{
  "name": "@restqa/leak-hunter",
  "version": "1.0.0",
  "description": "A Job that looking for API credential leaks across different platforms (ex: github).",
  "main": "hunter",
  "scripts": {
    "lint:fix": "standard --fix",
    "lint": "standard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "security",
    "credential",
    "search",
    "leakage"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.5",
    "yargs": "^15.0.2"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^2.0.1",
    "standard": "^14.3.1"
  }
}
